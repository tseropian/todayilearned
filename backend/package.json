{
  "name": "til-api",
  "description": "Unified Today I Learned backend — all Lambdas in one Serverless service / API Gateway.",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1121.0",
    "@aws-sdk/client-s3": "^3.1121.0",
    "@aws-sdk/lib-dynamodb": "^3.1121.0",
    "date-fns": "^4.4.0",
    "uuid": "^14.0.2"
  },
  "devDependencies": {
    "serverless": "^4.41.1",
    "serverless-offline": "^14.8.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "serverless"
    ]
  }
}
